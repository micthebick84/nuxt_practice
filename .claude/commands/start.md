---
description: Start the Nuxt development server
---

Start the Nuxt development server.

Steps:
1. Check if port 3000 is already in use
2. If port 3000 is in use, ask if user wants to kill existing process
3. Start the dev server with `npm run dev` in background
4. Confirm the server is running on http://localhost:3000
